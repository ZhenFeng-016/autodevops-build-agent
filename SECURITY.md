# Security policy

Report vulnerabilities privately to the repository owner. Do not open a public issue containing credentials, exploit details, or customer deployment data.

The repository must never contain npm credentials, GitHub tokens, SSH private keys, recovery codes, `.env` files, or production connection strings. Publishing uses npm Trusted Publishing/OIDC and GitHub-hosted runners.
