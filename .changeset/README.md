# Changesets

Add a Changeset for every public package behavior, protocol, or packaging change:

```bash
npm run changeset
```

The four public packages use a fixed release group so their compatibility version advances together. Internal private build packages are excluded from publishing.
